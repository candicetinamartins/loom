# MemPalace Python Bridge Requirements
# Install: pip install -r requirements-mempalace.txt

# Core MemPalace (when available)
# mempapalace>=1.0.0

# HTTP service
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Note: MemPalace itself is not yet on PyPI.
# Install from GitHub when ready:
# pip install git+https://github.com/milla-jovovich/mempalace.git
# 
# Until then, this service will report "mempalace not installed"
# and Tier 2 memory will fall back to the stub implementation.
