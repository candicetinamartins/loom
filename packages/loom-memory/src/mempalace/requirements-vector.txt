# MemPalace Python Bridge Requirements
# Install: pip install -r requirements-mempalace.txt

# FastAPI for HTTP service
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Vector database (standalone, no external mempaplace needed)
chromadb>=0.4.18
sentence-transformers>=2.2.2

# Note: This service now uses ChromaDB directly for vector storage.
# The external 'mempalace' package is NOT required.
